import XCTest
@testable import UnderlyingViewForSwiftUI

final class UnderlyingViewForSwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UnderlyingViewForSwiftUI().text, "Hello, World!")
    }
}
